# SourcePack skill repository
Build a small installable source-inspection skill around existing extractors. Preserve the sourcelens interfaces and evidence contracts. Do not restore the old platform roadmap.

Use behavior tests before changing runtime mechanics. Run `PYTHONPATH=skills/sourcepack/scripts python3 -m unittest discover -s tests -v`. Preserve original evidence and explicit gaps. Never auto-install source-derived instructions or commit user evidence. Runtime changes must work when only skills/sourcepack is copied to another directory. No hosted inference, provider-key reading, or silent paid fallback.
