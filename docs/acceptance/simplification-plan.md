Bounded plan: add inspect/finish convenience over bounded leased core inspections; v2 host results support explicit no-findings reason. Keep v1 APIs unchanged. Build derived caption reading view only, original spans/cue maps immutable. Add ranked relaxed lexical retrieval when strict matching fails, label fallback. Test tampering, foreign/uninspected citations, resumed/idempotent finish, caption repetition and retrieval. Fresh copied-skill baseline comparisons use frozen cases; publish measured limits and small experimental release only after checks.
